Verification trigger for FaceSwap Pro v1.3.2 Android packaging fix.

This branch verifies that the expression engine is packaged as faceswap_engine.py so the app no longer fails with a missing /files/app/faceswap.py path on Android.
